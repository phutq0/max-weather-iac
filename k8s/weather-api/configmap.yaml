apiVersion: v1
kind: ConfigMap
metadata:
  name: weather-api-config
  labels:
    app: weather-api
data:
  APP_ENV: "staging"
  LOG_LEVEL: "info"
  WEATHER_API_BASE_URL: "https://api.example.com"
  PORT: "8080"

